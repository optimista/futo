import { PasswordResetForm } from 'auth/reset'
import { FocusLayout } from 'layouts'

const AccountRecover = () => {
  return (
    <FocusLayout maxWidth="xs">
      <PasswordResetForm />
    </FocusLayout>
  )
}

export default AccountRecover;
